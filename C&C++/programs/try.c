#include <stdio.h>
#define format d

int main()
{
 long long int verybig;
 long int big;
 int normal;
//  long double doub;
 
 //printf("long long:%format\nlong:%format\nint:%format\n",sizeof(verybig),sizeof(big),sizeof(normal));
 printf("long double:%d\n",sizeof(big));
    

    return 0;
}